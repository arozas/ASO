Esto permite tener una máquina en virtualbox con Docker.

1. Instalar virtualbox (https://www.virtualbox.org/wiki/Downloads)
2. Instalar VirtualBox 6.1.34 Oracle VM VirtualBox Extension Pack
3. Instalar Vagrant (https://www.vagrantup.com/downloads)
4. Bajar el archivo Vagrantifle
5. Ejecutar donde se bajo el archivo Vagrantfile:

    vagrant up

Veremos que se genera en virtualbox la maquina virtual.
Para ingresar desde Vagrant:
     vagrant ssh

Desde la Virtual:

    Usuario    : vagrant
    Password : Vagrant